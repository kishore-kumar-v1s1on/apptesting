// Migrated to TypeScript — see ShopDetailsScreen.tsx
// This shim ensures both extensions resolve to the same module
// regardless of Metro/TS resolution order. Safe to delete once
// you've confirmed your tooling prefers .tsx.
export { default } from "./ShopDetailsScreen.tsx";
